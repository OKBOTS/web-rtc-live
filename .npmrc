auto-install-peers=false
strict-peer-dependencies=false

# Hoist platform-specific native binaries so rollup and esbuild can find them
# via require() regardless of pnpm's strict module isolation.
public-hoist-pattern[]=@rollup/rollup-*
public-hoist-pattern[]=@esbuild/*
